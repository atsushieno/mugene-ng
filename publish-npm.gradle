
var npmAuthToken = System.getenv('NPM_AUTH_TOKEN') ?: ''

npmPublish {
  //nodeBin = "node"
  //npmBin = "npm"
  readme = file("../README.md")
  organization = "dev.atsushieno"
  registries {
    register("npmjs") {
      uri.set(uri("https://registry.npmjs.org"))
      authToken = npmAuthToken
    }
  }
}
